{% extends 'base.html.twig' %}

{% block body %}

    <div class="row justify-content-center">
        <div class="col-md-8 mt-4">

            <h2>Formulaire des genres de livre</h2>

            {{ form_start(bookGenreForm) }}

            {{ form_widget(bookGenreForm) }}
            <button type="submit" name="submit" class="btn btn-danger">
                {{ buttonLabel }}
            </button>

            {{ form_end(bookGenreForm) }}

        </div>
    </div>

{% endblock %}

{% block title %}

{% endblock %}

